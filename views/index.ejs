        <% if (typeof items !== 'undefined' && items.length > 0) { %>
          <% items.forEach(item => { %>
            <%- include('card', { item: item }) %>
          <% }) %>
        <% } %>

        <% if (typeof item !== 'undefined') { %>
            <%- include('cardDetail', { item: item }) %>
        <% } %>